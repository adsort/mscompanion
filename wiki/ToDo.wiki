#labels Featured,Phase-Design
#work to do before the version 1.0 final version release  

= Introduction =

The 1.0 Alpha version is just a start, there are many work left to do before the first official version 1.0 come up.


= To Do list =

==Version 1.0==

  # support data source type detect and CONNECTION string building (now only support shape file)
  # join: let it more smart
  # Layer: for Feature Grid and Data, need to consider the excluding relationship
  # Suppoert Raster data type detect
  # export mapfile strictly complying to the version specified, now just distinguish <5.0 and >=5.0 (Finished)
  # get the default version according to the specified shp2img.exe (Finished)

 